var weight;
var height = 10;
console.log(height);
console.log(weight);
weight = 20;
console.log(weight);

// as the interpreter works line by line so when the weight is declared first and secondly should be assigned to print in the terminal or else it would be undefined.